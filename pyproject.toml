[project]
name = "uakino-scraper"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "scrapy>=2.14.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["UP031"]